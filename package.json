{
  "name": "fenshot-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w fenshot",
    "test": "npm run test -w fenshot",
    "dev": "vite --config apps/web/vite.config.ts apps/web"
  }
}
