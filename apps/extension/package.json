{
  "name": "extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-ort.mjs",
    "dev": "vite build --watch",
    "prebuild": "node scripts/sync-ort.mjs",
    "build": "tsc -p tsconfig.json --noEmit && vite build"
  },
  "dependencies": {
    "@scoriiu/fenshot": "^0.1.3",
    "chess.js": "^1.0.0",
    "onnxruntime-web": "1.26.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
